# @package experiment
name: "example"
trainer_args:
  max_epochs: 5
  gradient_clip_val: 1.0
dataset:
  _target_: data.TaxoBERTDataModule
  data_root: ${experiment.data_root}
  batch_size: 64
scheduler: ~
